/**
 * ConfigKey enum
 * @description These keys are used to store the configuration of the application.
 * @property {string} APP - The application config key.
 * @property {string} GEMINI - The gemini config key.
 */
export enum ConfigKey {
  APP = 'app',
  GEMINI = 'gemini',
}
