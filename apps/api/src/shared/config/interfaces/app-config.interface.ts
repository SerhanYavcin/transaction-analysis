/**
 * AppConfig interface
 * @description This interface is used to store the configuration of the application.
 * @property {number} port - The port of the application.
 */
export interface AppConfig {
  port: number;
}
