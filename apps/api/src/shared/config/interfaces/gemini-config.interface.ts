/**
 * GeminiConfig interface
 * @description This interface is used to store the configuration of the gemini model.
 * @property {string} apiKey - The api key of the gemini model.
 * @property {string} model - The model of the gemini model.
 */
export interface GeminiConfig {
  apiKey: string;
  model: string;
}
